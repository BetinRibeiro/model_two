{{extend 'layout.html'}}

<div class="py-5" >
    <div class="container">
      <div class="row">
        <div class="px-5 col-md-8 text-center mx-auto">
          <h3 class="text-primary display-4"> <b>{{=cab.titulo}}</b> </h3>
          <h2 class="my-3">{{=cab.sub_titulo}}.</h2>
          <p class="mb-3">{{=cab.texto}}.</p> 
            <a href="{{=cab.link}}" class="btn btn-primary">{{=cab.titulo_botao}}</a>
        </div>
      </div>
    </div>
  </div>

  <div class="py-5">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="row">
            <div class="col-md-4 col-lg-3 order-1 order-md-2 p-0"> 
              <img class="img-fluid d-block  animate-in-left" src="{{=URL('download', args=art[0].imagem)}}" />
              </div>
            <div class="d-flex flex-column justify-content-center p-3 col-md-8 offset-lg-1 align-items-start order-2 order-md-2">
              <h3>{{=art[0].titulo}}</h3>
              <p class="mb-3">{{=art[0].texto}}.</p> <a class="btn btn-primary" href="{{=art[0].link}}">{{=art[0].titulo_botao}}</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="py-5">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="row">
            <div class="d-flex flex-column justify-content-center p-3 col-md-8 offset-lg-1 align-items-start order-2 order-md-1">
              <h3>{{=art[1].titulo}}</h3>
              <p class="mb-3">{{=art[1].texto}}.</p> <a class="btn btn-primary" href="{{=art[0].link}}">{{=art[1].titulo_botao}}</a>
            </div>
            <div class="col-md-4 col-lg-3 order-1 order-md-2 p-0"> 
              <img class="img-fluid d-block animate-in-right" src="{{=URL('download', args=art[1].imagem)}}" /></div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="py-3 text-center">
    <div class="container">
      <div class="row">
        <div class="col-md-12 text-center">
          <h1>Características</h1>
        </div>
      </div>
      <div class="row justify-content-center">
        <div class="col-md-4 p-4"> <i class="d-block fa fa-circle fa-3x mb-2 text-muted "></i>
          <h4> <b>{{=car[0].titulo}}</b> </h4>
          <p>{{=car[0].texto}}.</p>
        </div>
        <div class="col-md-4  p-4"> <i class="d-block fa fa-stop-circle-o fa-3x mb-2 text-muted"></i>
          <h4> <b>{{=car[1].titulo}}</b> </h4>
          <p>{{=car[1].texto}}.</p>
        </div>
        <div class="col-md-4  p-4"> <i class="d-block fa fa-circle-o fa-3x mb-2 text-muted"></i>
          <h4> <b>{{=car[2].titulo}}</b> </h4>
          <p>{{=car[2].texto}}.</p>
        </div>
      </div>
    </div>
  </div>
  <div class="py-5 text-center">
    <div class="container">
      <div class="row">
        <div class="mx-auto col-md-12">
          <h1 class="mb-3">Equipe</h1>
        </div>
      </div>
      <div class="row">
        <div class="col-lg-3  p-4"> <img class="img-fluid d-block mb-3 mx-auto rounded-circle" src="{{=URL('download', args=des[0].imagem)}}" width="100" alt="Card image cap">
          <h4> <b>{{=des[0].nome}}</b> </h4>
          <p>{{=des[0].funcao}}</p>
          <p class="mb-0"> <i>"{{=des[0].frase}}"</i> </p>
        </div>
        <div class="col-lg-3 p-4"> <img class="img-fluid d-block mb-3 mx-auto rounded-circle" src="{{=URL('download', args=des[1].imagem)}}" width="100" alt="Card image cap">
          <h4> <b>{{=des[1].nome}}</b> </h4>
          <p>{{=des[1].funcao}}</p>
          <p class="mb-0"> <i>"{{=des[1].frase}}"</i> </p>
        </div>
        <div class="col-lg-3 p-4"> <img class="img-fluid d-block mb-3 mx-auto rounded-circle" src="{{=URL('download', args=des[2].imagem)}}" width="100">
          <h4> <b>{{=des[2].nome}}</b> </h4>
          <p>{{=des[2].funcao}}</p>
          <p class="mb-0"> <i>"{{=des[2].frase}}"</i> </p>
        </div>
        <div class="col-lg-3  p-4"> <img class="img-fluid d-block mb-3 mx-auto rounded-circle" src="{{=URL('download', args=des[3].imagem)}}" width="100">
          <h4> <b>{{=des[3].nome}}</b> </h4>
          <p>{{=des[3].funcao}}</p>
          <p class="mb-0"> <i>"{{=des[3].frase}}"</i> </p>
        </div>
      </div>
    </div>
  </div>
